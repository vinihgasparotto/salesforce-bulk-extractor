# ── Auth method ───────────────────────────────────────────────────────────────
# "password" = username + password + security token (no Connected App needed)
# "oauth"    = browser OAuth flow (no Connected App needed — uses PKCE like Data Loader)
SF_AUTH_METHOD=oauth

SF_LOGIN_URL=https://login.salesforce.com
SF_API_VERSION=59.0
SF_OUTPUT_DIR=output
SF_CALLBACK_PORT=8080

# ── Password auth (SF_AUTH_METHOD=password) ───────────────────────────────────
SF_USERNAME=you@example.com
SF_PASSWORD=yourpassword
SF_SECURITY_TOKEN=yourtoken   # leave blank if your IP is trusted in Salesforce

# ── OAuth with your own Connected App (optional) ──────────────────────────────
# Leave these blank to use Salesforce Data Loader's built-in public client (recommended)
SF_CLIENT_ID=
SF_CLIENT_SECRET=
